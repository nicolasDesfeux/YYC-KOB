dao.type=GSheet
gsheet.spreadsheet.id=1gwQcMQZ_Cibusv_6deeVgKBSgnhRuUy3mU8u9Y-bF_g
html.output.path=dashboard.html
# --- Scoring parameters (optional; omit any key to use the default shown) ---
# scoring.initial.score=50       Starting master score for a new or returning player
# scoring.minimum.players=8      Attendance gate for a game to count
# scoring.range.margin=10        Padding above/below the field's quartile averages
# scoring.recent.window=8        Bucket width: recent <= W, mid W+1..2W-1, old >= 2W
# scoring.quartile.divisor=4     Top/bottom sample size (4 -> quartiles)
# scoring.limit.to.year=true     Discard results older than one year
